import 'package:flutter/material.dart';

class WorkerProfilePage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Worker Profile Page'),),
    );
  }

}